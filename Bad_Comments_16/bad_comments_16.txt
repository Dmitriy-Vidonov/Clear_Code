1) Обязательные комментарии

public static int[] getConquer(int[][] map,    //получаем массив для проверки ячеек
                                      int x1, int y1, //получаем координаты точки
                                      int ignoreF1,   //получаем значение ignoreFlag
                                      int mapRows1,   //получаем число строк массива
                                      int mapCols1) { //получаем число столбцов массива

Вместо описания в комментариях каждой переменной следует дать более говорящие сами за себя имена переменных.
А лишние комментарии удалить.Они только загромождают код


2) Избыточный комментарий

// Если ignoreF == 1, то ставим в ячейки 2 и наоборот
        if (ignoreF1 == 1) cellFill = 2;
        else cellFill = 1;

Здесь комментарий просто можно удалить - он излишен.


3) Обязательные комментарии

public static int ConquestCampaign(int N, int M, int L, int[] battalion){
int days = 0; //число дней
int countFull = 0; //переменная для подсчета заполненных ячеек
int ignFlag; //флаг для игнорирования содержимого ячеек

Здесь в общем случаи данные комментарии можно попросту удалить.


4) Избыточный комментарий

for (int n = 0; n < N; n++) { //внешний цикл по строкам
            for (int m = 0; m < M; m++) { //внутренний цикл по строкам
                map[n][m] = 0;
            }
        }

Данные пояснения по циклам так же избыточны. Их можно заменить одним общим комментарием перед началом главного цикла.


5) Избыточный комментарий

// Если ignoreF == 1, то ставим в ячейки 2 и наоборот
        if (ignoreF1 == 1) cellFill = 2;
        else cellFill = 1;

Здесь и без комментария очевидно, что происходит в коде


6) Закомментированный код

 /*ручной тест с выводом корявых данных
        int count = 0;
        int N1 = 7;
        for (int i=0; i<500; i++) {
            int[] arrTest = Methods.generateArray(N1, 0, 255);
            int[] arr = ForTest2.forTest2(arrTest, N1);
...
*/

Подлежит удалению


7) Закомментированный код

 //System.out.println("arrF после смены элементов:");
 //for(int count : arrF) System.out.print(count + " "); System.out.println();

Подлежит удалению


8) Закомментированный код

  /*проверить результат выполнения - выведем массив matrix на экран
        for(int i1=0; i1<matrix.length; i1++) {
            for(int j1=0; j1<matrix[0].length; j1++) {
                System.out.print(matrix[i1][j1]);
            }
            System.out.println();
        }*/

Подлежит удалению


9) Обязательные комментарии

int rows = (int) Math.sqrt(len); //число строк матрицы
int cols = (int) Math.ceil(Math.sqrt(len)); //число столбцов матрицы

Эти комментарии можно просто удалить.


10) Обязательные комментарии

 num1 = "0"; //первое число
        num2 = "0"; //второе число

        int len1, len2, countLen, countLetter;
        len1 = num1.length(); //длина первого числа
        len2 = num2.length(); //длина второго числа

их в целом можно удалить, наглядность кода от этого не пострадает


11) Обязательные комментарии

int[] num1Array = new int[len1]; //объявление массива для 1-ого числа
int[] num2Array = new int[len2]; //объявление массива для 2-ого числа

Подлежат удалению


12) Обязательные комментарии

//1) найти max элемент
        float maxElem = arr[0];
        for(int i=0; i<arr.length; i++) {
            if(maxElem < arr[i]) {
                maxElem = arr[i];
            }
        }

Данный комментарий можно удалить, так как наименование переменной исчерпывающе


13) Закомментированный код

//Тестовый вывод итогового массива
//for(int nums : numArray) System.out.print(nums + " "); System.out.println();

Подлежит удалению


14) Закомментированный код

/*
 public static void main(String[] args) {
 int[][] track = {{11,5,5}, {15,2,2}};
 int L = 10;
 int N = 2;
 System.out.println("t = " + Unmanned(L, N, track));
 }
 */

Код для примера тестового запуска. В конечном коде не нужен, подлежит удалению.


15) Обязательные комментарии

int H1 = 3; //число строк первого массива
int W1 = 4; //число столбцов первого массива
String S1 = "1234 2345 0987"; //задание строки для первого массива

int H2 = 2; //число строк второго массива
int W2 = 2; //число столбцов второго массива

Данные комментарии можно попросту удалить, назвав переменные более понятными именами, говорящими сами за себя.




